import { DynamicWidthDirective } from './dynamic-width.directive';

describe('DynamicWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicWidthDirective();
    expect(directive).toBeTruthy();
  });
});
